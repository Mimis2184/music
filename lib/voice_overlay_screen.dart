import 'package:flutter/material.dart';

class VoiceOverlayScreen extends StatelessWidget {
  const VoiceOverlayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Voice Overlay Screen")),
    );
  }
}
